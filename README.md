# Новостной сайт на Django

Веб-приложение для публикации и управления новостями с использованием Django, SQLite и uv.

## 📋 Описание проекта

Проект реализует полноценную новостную платформу с:
- **Публичной частью**: просмотр новостей, поиск по заголовкам и содержанию
- **Административной частью**: CRUD-операции через админ-панель Django
- **Адаптивным интерфейсом**: современный дизайн с использованием CSS

## 🛠 Технологии

- **Backend**: Django 6.0+
- **База данных**: SQLite (для разработки)
- **Менеджер пакетов**: uv (альтернатива pip)
- **Шаблонизатор**: Django Templates
- **Стилизация**: CSS (встроенные стили в шаблонах)

## 🚀 Быстрый старт

### Предварительные требования
- Python 3.11 или выше
- Git

### Клонирование репозитория
```bash
git clone <ваш-репозиторий>
cd web-news
```

# Создайте файл .env из шаблона
```bash
cp .env.example .env
```
# Отредактируйте .env при необходимости
```bash
nano .env  # или используйте любой текстовый редактор
```
# Создайте файл .env из шаблона
```bash
cp .env.example .env
```
# Отредактируйте .env при необходимости
```bash
nano .env  # или используйте любой текстовый редактор
```
# Используйте uv
```bash
uv sync
```
# Или используйте pip
```bash
pip install django python-dotenv
```
# Примените миграции
```bash
python manage.py migrate
```
# Создайте администратора
```bash
python manage.py createsuperuser
```
# Следуйте инструкциям в терминале
```bash
python manage.py runserver
```